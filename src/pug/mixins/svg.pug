include ../mixins/timestamp

mixin svg(name, width, height, styles)
	svg(width=width || '20px', height=height || '20px', style=styles || '')
		use(xlink:href="/static/icons/sprite.symbol.svg" + timestamp + '#' + name)